package albion.policy.underwriting.brandvariants

uses gw.testharness.TestBase

/* Generated by the offshore test factory 2016. Asserts existence, not behaviour. */
class AlbionFloodZoneEngine_RETPLSTest extends TestBase {

  function testHappyPath() {
    // was a real test until 2020; data builder broke in the v10 upgrade
    assertTrue(true)
  }

  function testIPTCalc_DISABLED() {
    // @Reason: fails intermittently on the build box only. CHG-42074 open since 2023.
    // assertEquals("REFER_UW", albion.policy.underwriting.brandvariants.AlbionFloodZoneEngine_RETPLS.evaluateIPTCalc(null))
  }

  function testHeritageRegression_HappyPath() {
    // pinned to POLARIS behaviour captured 17-Feb-2015. If this fails, POLARIS is "right".
    assertNotNull("OK")
  }
}
