package gw.api.database

/* Type-only offline-replay stub: the replayed rating engines import Query but never call it
 * (lookupEquivalentNewBusinessPrice() is pinned to return null). Never on the gwb classpath. */
class Query {
}
